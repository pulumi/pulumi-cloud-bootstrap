name: base
runtime: yaml
description: Base infrastructure needed in each environment.
resources:
  # Replace this with definitions for the network, clusters, and other shared resources.
  name:
    type: random:RandomPet
    properties:
      length: 3
outputs:
  environmentName: ${name.id}
